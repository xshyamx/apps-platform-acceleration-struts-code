---
resources:
- name: apps-platform-acceleration-struts-code
  type: git
  source:
    uri: {{github-repository}}
    branch: {{github-branch}}
    private_key: {{github-private-key}}

- name: apps-platform-acceleration-struts-code-artifacts
  type: s3
  source:
    bucket: {{aws-bucket}}
    regexp: releases/struts-(.*).war
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: version
  type: semver
  source:
    bucket: {{aws-bucket}}
    key: apps-platform-acceleration-struts-code/version
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: sandbox-deployment
  type: cf
  source:
    api: {{cf-api-url}}
    username: {{cf-username}}
    password: {{cf-password}}
    organization: {{cf-org}}
    space: sandbox


jobs:
- name: build
  plan:
  - get: apps-platform-acceleration-struts-code
    trigger: true
  - get: version
    params: {bump: patch}
  - task: build
    file: apps-platform-acceleration-struts-code/ci/build.yml
  - put: apps-platform-acceleration-struts-code-artifacts
    params:
      file: build-output/struts-*.war
  - put: version
    params:
      file: version/number

- name: deploy-review
  plan:
  - get: apps-platform-acceleration-struts-code
    passed: [build]
  - get: apps-platform-acceleration-struts-code-artifacts
    trigger: true
    passed: [build]
  - put: sandbox-deployment
    params:
      manifest: apps-platform-acceleration-struts-code/manifest.yml
      path: apps-platform-acceleration-struts-code-artifacts/struts-*.war

